import { createClient } from '@supabase/supabase-js';
import dotenv from 'dotenv';
import fs from 'fs';
import path from 'path';

// Load environment variables
dotenv.config();

// Get Supabase configuration
const supabaseUrl = process.env.VITE_SUPABASE_URL;
const supabaseAnonKey = process.env.VITE_SUPABASE_ANON_KEY;

if (!supabaseUrl || !supabaseAnonKey) {
  console.error('❌ Missing Supabase configuration');
  process.exit(1);
}

// Create Supabase client
const supabase = createClient(supabaseUrl, supabaseAnonKey);

async function testPhotoUpload() {
  console.log('\n=== Testing Photo Upload Functionality ===\n');
  
  try {
    // 1. Check if storage buckets exist
    console.log('1. Checking storage buckets...');
    const { data: buckets, error: bucketsError } = await supabase.storage.listBuckets();
    
    if (bucketsError) {
      console.error('❌ Failed to list buckets:', bucketsError.message);
      return;
    }
    
    console.log('✅ Available buckets:');
    buckets.forEach(bucket => {
      console.log(`   - ${bucket.name} (${bucket.public ? 'public' : 'private'})`);
    });
    
    // Check for specific buckets
    const newsPhotoBucket = buckets.find(b => b.name === 'news-photos');
    const eventPhotoBucket = buckets.find(b => b.name === 'event-photos');
    
    if (!newsPhotoBucket) {
      console.log('⚠️  news-photos bucket not found');
    } else {
      console.log('✅ news-photos bucket exists');
    }
    
    if (!eventPhotoBucket) {
      console.log('⚠️  event-photos bucket not found');
    } else {
      console.log('✅ event-photos bucket exists');
    }
    
    // 2. Test file upload to news-photos bucket (if it exists)
    if (newsPhotoBucket) {
      console.log('\n2. Testing file upload to news-photos bucket...');
      
      // Create a minimal valid JPEG file (1x1 pixel)
      const jpegHeader = Buffer.from([
        
      ]);
      const fileName = `test-news-${Date.now()}.jpg`;
      
      const { data: uploadData, error: uploadError } = await supabase.storage
        .from('news-photos')
        .upload(fileName, jpegHeader, {
          contentType: 'image/jpeg'
        });
      
      if (uploadError) {
        console.error('❌ Upload to news-photos failed:', uploadError.message);
      } else {
        console.log('✅ Upload to news-photos successful:', uploadData.path);
        
        // Get public URL
        const { data: urlData } = supabase.storage
          .from('news-photos')
          .getPublicUrl(fileName);
        
        console.log('   Public URL:', urlData.publicUrl);
        
        // Clean up
        await supabase.storage.from('news-photos').remove([fileName]);
        console.log('✅ Test file cleaned up');
      }
    }
    
    // 3. Test file upload to event-photos bucket (if it exists)
    if (eventPhotoBucket) {
      console.log('\n3. Testing file upload to event-photos bucket...');
      
      // Create a minimal valid JPEG file (1x1 pixel)
      const jpegHeader = Buffer.from([
        
      ]);
      const fileName = `test-event-${Date.now()}.jpg`;
      
      const { data: uploadData, error: uploadError } = await supabase.storage
        .from('event-photos')
        .upload(fileName, jpegHeader, {
          contentType: 'image/jpeg'
        });
      
      if (uploadError) {
        console.error('❌ Upload to event-photos failed:', uploadError.message);
      } else {
        console.log('✅ Upload to event-photos successful:', uploadData.path);
        
        // Get public URL
        const { data: urlData } = supabase.storage
          .from('event-photos')
          .getPublicUrl(fileName);
        
        console.log('   Public URL:', urlData.publicUrl);
        
        // Clean up
        await supabase.storage.from('event-photos').remove([fileName]);
        console.log('✅ Test file cleaned up');
      }
    }
    
    console.log('\n=== Photo Upload Test Complete ===');
    
  } catch (error) {
    console.error('❌ Test failed with error:', error.message);
    console.error('Stack:', error.stack);
  }
}

// Run the test
testPhotoUpload();